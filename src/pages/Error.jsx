import styles from './Error.module.css';

const Error = () => {
  return (
    <div>
      <h1 className={styles.errorTitle}>Yщu moved non-existing page</h1>
    </div>
  );
};

export default Error;
